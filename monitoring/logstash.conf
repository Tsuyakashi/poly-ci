input {
  beats {
    port => 5044
  }
}

filter {
  # Тут в будущем можно добавить разбор JSON логов Nginx (grok или json filter)
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "nginx-logs-%{+YYYY.MM.dd}"
  }
}
